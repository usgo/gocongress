- model_class = Round

.page-header
  %h2= t '.title', :default => model_class.model_name.human.pluralize.titleize
%table.semantic.fullwidth
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:tournament)
      %th= model_class.human_attribute_name(:number)
      %th= model_class.human_attribute_name(:start_time)
      %th= model_class.human_attribute_name(:created_at)
      %th= t '.actions', :default => t("helpers.actions")
  %tbody
    - @rounds.each do |round|
      %tr
        %td= link_to round.id, round_path(round)
        %td= round.tournament.name
        %td= round.number
        %td= round.start_time
        %td= l round.created_at
        %td
          = link_to "Edit", edit_round_path(round)
          = link_to "Delete",                                           |
            round_path(round),                     |
            :method => :delete,                                          |
            :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },  |
            :class => 'btn btn-xs btn-danger'                            |

= link_to "New", new_round_path, :class => 'button'
= form_for @import, url: import_rounds_path, mulitpart: true do |f|
  %fieldset
    %h2 Import Round
    .field
      = f.file_field :file
      = f.submit "Upload"
