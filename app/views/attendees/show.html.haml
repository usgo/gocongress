- a = @attendee

%h2= a.get_full_name

/ It is helpful to administrators if there is an easy link
/ back to the user account page.  Naturally, regular users
/ do not need such a link.
- if can?(:manage, a.user) && a.user != current_user
  %p
    #{a.given_name.titleize} is an #{Attendee.model_name.human.downcase}
    on the acount: 
    = link_to a.user.email, a.user

%ul
  %li= link_to 'Basics', edit_attendee_path(a, :basics)
  %li= link_to 'Player Info', edit_attendee_path(a, :baduk)
  %li= link_to 'Room and Board', edit_attendee_path(a, :roomboard)
  %li= link_to 'Tournaments', edit_attendee_path(a, :tournaments)
  %li= link_to 'Events', edit_attendee_path(a, :events)

- if current_user.is_admin?
  %h3 Administration
  %ul
    %li= link_to 'Admin', edit_attendee_path(a, :admin)
    %li= link_to 'Print Registration Sheet', print_summary_for_attendee_path(a)
    %li= link_to 'Print Badge', print_badge_for_attendee_path(a)
