%fieldset
  .field
    = f.label :phone
    = f.text_field :phone

  -# For the primary attendee, the email has already been copied
  -# from the user record.  However, if they change the email in
  -# the user record, the primary attendee's email is not
  -# automatically updated, so we need a field here also, for now.
  .field
    = f.label :email
    = f.text_field :email

  -# The `country_select` method is provided by the country-select gem
  .field
    = f.label :country
    = f.country_select :country, PRIORITY_COUNTRIES, { :include_blank => true }
