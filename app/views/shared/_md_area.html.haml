/ Parameters: 
/ obj - a symbol naming an activerecord model
/ atr - a symbol naming an attribute of said model

%label{ :for => obj.to_s + '_' + atr.to_s}
  = atr.to_s.titleize
  %br
  %span.smalltext
    You can format the text (bold, etc.) with
    %a{ :href => 'http://en.wikipedia.org/wiki/Markdown', :target => '_blank' } Markdown
    syntax.
= text_area obj, atr, { :class => 'wide', :rows => 5 }
