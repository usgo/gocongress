- mnh = ActivityCategory.model_name.human
- mnhp = mnh.pluralize

%h2= page_title

- if can? :create, ActivityCategory
  .flow-padding= button_to "New #{mnh}", new_activity_category_path, :method => "get"

- if @activity_categories.empty?
  %p There are no #{mnhp.downcase} yet.
- else
  %table.semantic.zebra.fullwidth
    %tr
      %th Category
      %th
      %th
  
    - @activity_categories.each do |c|
      %tr
        %td= link_to c.name, c
        %td
          -if can? :update, c
            = link_to 'Edit', edit_activity_category_path(c)
        %td
          -if can? :delete, c
            = link_to 'Delete', c, :confirm => 'Are you sure?', :method => :delete

%br
