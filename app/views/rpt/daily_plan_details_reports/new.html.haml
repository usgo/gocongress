%h2 Daily Plan Details
%p
  A <strong>daily plan</strong> allows <strong>attendees</strong> to
  pick a set of dates, and charges a daily rate.

= form_tag(rpt_daily_plan_details_report_path, {method: 'post'}) do
  %p
    - opts = options_from_collection_for_select(@daily_plans, "id", "name")
    = select_tag :plan_id, opts, {include_blank: false}
  %p= button_to "Download CSV", rpt_daily_plan_report_path + '.csv', :method => "get"

