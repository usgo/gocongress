%h1 Cost Summary

%p
  Your initial deposit is due
  =@user.get_initial_deposit_due_date.to_formatted_s(:long)

%table.semantic.fullwidth
  %thead
    %tr
      %th Attendee
      %th Description
      %th.numeric Price
  %tbody
    - for i in @invoice_items
      %tr
        %td= i['attendee_full_name']
        %td= i['item_description']
        %td.numeric= number_to_currency i['item_price']
    %tr.total
      %td
      %th Total
      %td.numeric= number_to_currency @user.get_invoice_total
